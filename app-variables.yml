variables:
  #user must update these each build personally as new versions are being worked on
  Major: '2'
  Minor: '2'
  Patch: '0'

  # set these once and forget them per project/repo 
  HasSqlProj: 'yes'  #can be empty or ignored if unnecessary, but if "yes" then pipeline will find any *.sqlproj projects and build them 
  #PostTargetToNuGet: ''  #can be empty or ignored if unnecessary, but if "yes" then pipeline will build alpha, beta, and semantic NuGet release packages
  #TargetProject: '**/ProphetsWay.Example.csproj'
  Product: 'Example'
  RepoName: 'ProphetManX/ProphetsWay.Example'
  #NuGetFeedCredentialName: 'ProphetManX''s NuGet'
  GitHubConnectionName: 'ProphetsWay@GitHub'